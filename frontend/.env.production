# YSI Frontend - Production Environment
# Este archivo se usa para builds de producción (Vercel, etc.)

# Backend API URL - Production (Cloudflare Tunnel) - HTTPS
VITE_API_BASE_URL=https://bool-mic-acrylic-elliott.trycloudflare.com/api/v1

# Production settings
NODE_ENV=production
VITE_APP_TITLE=YSI Catalyst Admin
VITE_ENVIRONMENT=production

# Vercel deployment flag
VITE_VERCEL_ENV=production

# Disable debug in production
VITE_DEBUG=false